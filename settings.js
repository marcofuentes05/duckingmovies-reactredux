const ngrok_base = 'https://16b966753e32.ngrok.io' // EL URL DE NGROK VA AQUI
// -----------------------------------------------------------
export const API_BASE_URL = `${ngrok_base}/api/v1`
export const API_BASE_AUTH = `${ngrok_base}/api-token-auth/`
export const API_BASE_AUTH_REFRESH = `${ngrok_base}/api-token-refresh/`

export const validTimePercentage = 0.5;

export const tokenReviewTime = 300 * 1000; // Seconds * 1000
